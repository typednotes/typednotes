# Computations

Computations happen like in a spreadsheet (a big reactive dag) but where each cell is a module written in natural language with Lean formalized interface with Effect guarantees.

The spreadsheet of the AI era.

We should be able to display sources, sinks and relative dependencies (direct and closure)

To see how to combine with `bun` (running modules). And how the agent `lode` writes the code and pushes it.